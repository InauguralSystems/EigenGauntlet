# Fixture loaded by module_scope_lab.eigs.

load_scope_counter is 0
load_scope_label is "fixture-loaded"

define load_scope_get_label() as:
    return load_scope_label

define load_scope_bump() as:
    load_scope_counter is load_scope_counter + 1
    return load_scope_counter

define make_loaded_counter(start) as:
    loaded_count is start
    define next() as:
        loaded_count is loaded_count + 1
        return loaded_count
    return next
